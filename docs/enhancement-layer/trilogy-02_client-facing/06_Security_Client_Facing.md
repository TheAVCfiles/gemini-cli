# Security brief for clients

- Local-first by default: your data stays on your machine unless you opt-in for sync/backups.
- Signed exports: downloadable, auditable artifacts signed by your key.
- Optional managed relay: only encrypted blobs forwarded; relay cannot decrypt.
