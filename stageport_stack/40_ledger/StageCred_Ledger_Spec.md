# StageCred Ledger Spec (stub)

Anchor for ledger events such as `MoveMintEvent`, `CredentialMintEvent`, and `HumanIndexSnapshot`. Use alongside CTDL templates
in `50_credentials/`.

## Reference credential prototypes

- **Operator Literacy v1** (`50_credentials/Operator_Literacy_v1.json`): StageCred JSON credential representing proof-of-learning minted from operator uploads. Status: ACTIVE/VERIFIED. Invocation trigger: “I Am Learning”.
