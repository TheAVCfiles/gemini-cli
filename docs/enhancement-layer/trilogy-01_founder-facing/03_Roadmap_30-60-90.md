# Roadmap (30/60/90)

30 days:

- Local PWA (Automerge + IndexedDB) + First Value Wizard + signed snapshot export
- Relay server local scaffold + docker-compose, worker for send-app

60 days:

- Stateless relay -> enrichment worker -> LLM integration
- Grant portal one-click send integrated into client workspace
- Feature flags and basic A/B experiments

90 days:

- Tauri desktop packaging and local LLM option
- SLSA/cosign production CI, WebAuthn key management
- Pilot with 3 studios; onboarding & pricing refinement
